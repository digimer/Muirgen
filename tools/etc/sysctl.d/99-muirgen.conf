# Increase core receive buffer sizes (default and max)
net.core.rmem_default = 1048576
net.core.rmem_max = 4194304

# Increase the maximum packet backlog for the interface ring
net.core.netdev_max_backlog = 10000
